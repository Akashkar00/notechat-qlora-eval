# Data report — NoteChat (Phase 1)

Generated by `src/data/build_dataset.py`. Do not hand-edit — rerun the
script to regenerate after any pipeline change.

## Coverage

- Source file: `our_revised_v2.csv` (see `docs/DECISIONS.md` for why this
  variant was picked over NoteChat's other released files).
- Raw rows: 10000
- Exact-duplicate notes dropped (by content hash): 0
- Notes in the final dataset: 10000

## Splits (by `note_id`, seed 42 — see `configs/data.yaml`)

| Split | Notes | % |
|---|---|---|
| train | 8000 | 80.0% |
| val | 1000 | 10.0% |
| test | 1000 | 10.0% |

## Data-quality notes

- Conversations with a leading preamble before the first `Doctor:`/`Patient:` turn marker (stripped at build time): 18 (0.18%). This is a generation artifact in the source data (e.g. a leaked keyword list from the prompt), not a labeling error — see `docs/DECISIONS.md`.
- **Ground-truth caveat:** the `conversation` column is itself an LLM-generated synthetic dialogue (NoteChat's multi-agent pipeline output), not a human-authored or human-verified reference. Treat reference-based metrics (ROUGE/BLEU/BERTScore against this column) as *similarity to one synthetic exemplar*, not as ground truth in the sense CUAD's expert annotations were. See `PROJECT_SPEC.md` §4.2 and Phase 4.

## Clinical note length (chars)

- mean 1725.6, min 140, max 2330

## Conversation length (chars)

- mean 2313.6, min 857, max 5247

## Turns per conversation

- Total turns: mean 20.0, min 6, max 61
- Doctor turns: mean 10.6, min 4, max 31
- Patient turns: mean 9.5, min 0, max 30
